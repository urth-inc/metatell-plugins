# Required. OIDC client ID registered for the external API.
# The plugin passes this value to getPluginApiToken() as the requested token audience.
METATELL_CLIENT_ID=your-client-id

# Optional. Backend sample base URL.
# Defaults to http://localhost:3000 when omitted.
METATELL_EXTERNAL_API_BASE_URL=http://localhost:3000
